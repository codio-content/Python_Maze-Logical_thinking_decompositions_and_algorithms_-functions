{
// Configure your Run and Preview buttons here.

// Run button configuration
  "commands": {
    "Start Nodemon": "nodemon --ignore public/ -e js,ejs app.js",
    "Start Node": "node app",
    "Kill Node Processes": "pkill node & pkill nodemon"    
  },

// Preview button configuration
  "preview": {
        "Project Index (static)": "https://{{domain}}/{{index}}",
        "Current File (static)": "https://{{domain}}/{{filepath}}",
        "Box URL": "http://{{domain9500}}/",
        "Box URL SSL": "https://{{domain9500}}/"
  }
}